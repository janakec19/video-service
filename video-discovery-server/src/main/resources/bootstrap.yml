spring:
  application:
    name: video-discovery-server
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: jana123
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0